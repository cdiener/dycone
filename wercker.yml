box: rocker/hadleyverse
build:
  steps:
    - script:
        name: Install glpk lib
        code: |
            apt-get update
            apt-get install -y -t testing libglpk-dev 
    - inbobmk/r-dependencies
    - inbobmk/r-check
    - inbobmk/r-coverage
